#include "runtime.h"

int Call_int_ref_char_short_int_long_long_int_(char a, short b, int c, long long d, long long e, int f, void* __context)
{
	return a == -1 && b == -2 && c == -3 && d == -4 && e == -5 && f == -6;
}
