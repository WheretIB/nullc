#include "runtime.h"

int Call_int_ref_char_short_int_char_short_int_(char a, short b, int c, char d, short e, int f, void* __context)
{
	return a == -1 && b == -2 && c == -3 && d == -4 && e == -5 && f == -6;
}
